import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.p2ptrader.syria',
  appName: 'تاجر P2P',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
